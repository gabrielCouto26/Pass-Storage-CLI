{
  "name": "pass-storage",
  "version": "1.0.0",
  "description": "Simple CLI for encrypted passwords backup",
  "main": "src/index.ts",
  "type": "module",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "rm -r ./dist/ && tsc",
    "init": "bash ./initializer.sh"
  },
  "author": "Gabriel de Lima Couto",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.1.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "clipboardy": "^4.0.0",
    "commander": "^12.1.0"
  }
}
